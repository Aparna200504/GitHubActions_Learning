name: Build

on: 
  workflow_call: # whenever this workflow needs to be called from another

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build the code
        run: echo "Build the code"
